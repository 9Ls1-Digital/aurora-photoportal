# ADR-031 – Customer Account Views

Customer account navigation uses the permanent customer portal URL with an `account_view` query parameter. Profile is read-only. Status is calculated from existing project delivery state. The main portal remains the canonical projects/gallery overview.
