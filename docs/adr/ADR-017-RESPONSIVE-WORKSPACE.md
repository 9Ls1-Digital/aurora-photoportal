# ADR-017 — Responsive Workspace & Mobile Navigation

Status: Accepted for development

Aurora Photographer Workspace uses a desktop fixed sidebar and a mobile
off-canvas navigation. On mobile the same tenant-aware navigation remains
available behind a hamburger control, with an overlay and explicit close
behaviour. Workspace content uses the full viewport width and dense register
tables remain horizontally scrollable rather than being reduced to unreadable
columns.
