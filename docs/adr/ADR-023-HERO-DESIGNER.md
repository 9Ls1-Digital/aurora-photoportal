# ADR-023 — Hero Designer Foundation

Hero presentation is editable in the Photographer Workspace and stored separately for each customer portal and gallery. Settings include image selection, size, focal point, overlay color and opacity. All saved image selections are backend-validated against the current photographer account and owning customer/gallery. Public pages only consume validated tenant-scoped configuration.
